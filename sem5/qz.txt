border-radius	аттрибут, скругляющий края фона страницы

gap	расстояние между дочерними элементами структуры(flex,grid,?)

как объявляется шрифт(этап css)	описывается в @font-face; потом внутри селектора упоминается имя font-face;

Сколько font-family можно описывать внутри одного @font-face	1

Что ложить в source:local(item)	полное название шрифта в рамках операционной системы

Как связан @font-face и отдельный селекторы	под селекторами пишем font-family в точности как  в @font-face.

в каких еденицах лучше замерять grid	fr

аттрибут css для горизонтального скролла по оси (OY), в случае переполнения	overflow-y: scroll

в общем виде, операторы выравнивания для осей ox,oy 	align для оси ox, justify для оси oy

выравнивание "одной" ячейки	align-self, justify-self

виды записи пробела между ячейками(3 шт)	gap: ox oy; row-gap: ox; column-gap: oy;

вариант "сокращённой" записи	grid-row/*-column*/: {начальная линия} span n;

тег смыслового блока	section

выравнивание текста(и всё?)		text-align

тег и (в целом)класс "нижнего колонтитула"	footer

Правила заголовков 	их в целом делают по убыванию; от Хедера до футера спускаемся на 4-5 этажей

Способ центровки элемента	max-width;margin-left:auto;margin-right:auto;

Способ задать общий фон нескольким(связным) 	завернуть в ещё один div

Предпочтительный разделитель дочерних элементов	gap

Предпочтительный способ задать ширину	max-width

Проверка вёрстки	меняем масштаб, ломаться не должно.