Особенность шрифта по символам/региональным алфавитам	нужно убедиться что шрифт поддерживает нужные символы

2 обязательных аттрибута для font-face	font-family, source

Как подключить шрифт из файла	@font-face {source:url("./path/font.otf")}

Как подключить файл из библиотеки OC	@font-face {source:url("Точное-Имя-В-ОС")}

Основные(3) признак адаптивной вёрстки	отсутствие горизонтальной прокрутки, "независимо" от устройства; читабельный текст; 
большие области для элементов интерфейса

Применение width, max-width, min-width	min/max-width применяется к родительским элементам, для дочерних же _можно_ ставить фиксированную ширину

Веса применения min-max weight-height	чаще max-width,min-height; реже min-width,max-height

Как включить эмуляцию девайса(телефон, планшет)	в отладчике слева вверху "toggle device toolbar(иконка ноут-телефон)"; левее(над страницей) - пресеты разрешений

Какой тег в head отвечает за адаптивность	<meta name="viewport">

Основные аттрибуты viewport
width, height, пределы	указывают абсолютное выражение 100% размера дисплея; от 

Основные аттрибуты viewport
initial-scale, minimum-scale,maximum-scale	указывает начальный масштаб и пределы масштабирования

Основные аттрибуты viewport
user-scaleable	разрешает/запрещает пользователю масштабировать страницу

visual viewport, layout viewport, разница	v.viewport - видимые элементы ui; l.viewport - все элементы ui, видимые, и невидимые ( в этом масштабе)

Основные аттрибуты viewport
interactive-widget	описывает взаимодействие контента и интерактивных интерфейсов(клавиатура, н-р);

как может вести себя visual-viewport:interactive-widget	resizes-visual - менять visual wiewport /
resizes-content - менять layout viewport /
overlays-content - перекрывает visual viewport

Медиазапрос	условная конструкция, применяющая стили для определённых устройства

синтаксис медиазапроса	@media (условия)  {/* css аттрибуты */}

Логические операторы условий медиазапросов(перечисление) not,and,or, only

Альтернативная запись or	,(запятая)

Расшифровка only	and с обратной совместимостью для старых браузеров

