где смотреть отображение дизайна на других десктопных расширениях	отладчик, responsive device, меняем ширину

инструмент(ы) адаптивной вёрстки	только медиазапросы

инструмент(ы) отзывчивой( responsive) вёрстки	размер элементов в %

инструменты резиновой вёрстки	задаём элементам минимальные и максимальные размеры

особенность фиксированной(регламентированной) вёрстки	размер элементов задан в пикселях, при изменении размера экрана всё ломается

можно ли выносить медиазапросы в отдельный файл	да

можно ли задавать ширину для body	нет

как описывать максимальный размер экрана для медиазапроса	размер-1

что выносить в медиазапрос	только перезаписанные и новые аттрибуты

когда делаем перенос на что обращать внимание, главным образом	на элементы( позицию, размер, отступы(gap margin padding)), шрифты( размер) 

почему gap лучше margin	gap лучше адаптируется к изменению направления элементов

задаём ширину блоков в адаптиве	{max-width: 100%; margin-left: 0px; margin-right: 0px;}

как заставить элемент не занимать весь размер (flex) ячейки	выровнять по основной оси(align-items:center) 

что означает розовое выделение в отладчике	дочерний элемент выходит за пределы родительского

как описывать +/- одинаковые элементы	общий класс для сходств, где есть корректировки - добавляем дополнительный класс с ними

как надёжно описать одну колонку грид	{grid-template-columns: 1fr; grid-template-rows: auto;}  

{}